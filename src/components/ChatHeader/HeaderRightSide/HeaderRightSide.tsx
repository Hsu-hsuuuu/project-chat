import React, { FC } from 'react';
import styles from './HeaderRightSide.module.scss'

const HeaderRightSide: FC = () => {
    return (
      <div className={ styles.right_side }>
      </div>
    );
  }
  
export default HeaderRightSide;