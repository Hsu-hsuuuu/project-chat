import React, { FC, useState } from 'react';
import styles from './Chat.module.css';

const Chat: FC = () => {
  return (
    <div className={ styles.chat }>
      
    </div>
  );
}

export default Chat;
